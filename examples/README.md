# Examples

Version 1.0 generates example output through the built-in Tennis entry point:

```bash
python -m logical_puzzle_generator.create_puzzle
```

The command writes puzzle and solution PDFs to `output/` by default. Generated PDFs are runtime artifacts and are not committed as canonical source files.
